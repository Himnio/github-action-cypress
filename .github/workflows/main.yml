name: cypress-test

on: push

jobs:
  cypress-run:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: action/checkout@v4
      - name: Cypress run 
        uses: cypress-io/github-action@v6
        # with:
        #   build: npm run build
        #   start: npm start